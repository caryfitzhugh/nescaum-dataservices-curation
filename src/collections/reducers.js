import immutable from "object-path-immutable";

function collectionsReducer(state = INITIAL_RESOURCES_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export { collectionsReducer};
